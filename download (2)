import { useState, useRef } from 'react';
import { Heart, MapPin, Car, Users, Shield, Star, ChevronRight, Menu, X } from 'lucide-react';
import { Button } from '@/components/ui/button';

/**
 * DESIGN PHILOSOPHY: Ultra-Premium Luxury Concierge
 * - Trust-first visual language with minimal, elegant aesthetic
 * - Soft ivory base with champagne/gold accents
 * - Glassmorphism floating nav for mobile-app-like experience
 * - High whitespace, calm hierarchy, smooth micro-interactions
 * - Playfair Display for headings (editorial luxury), Geist for body (modern readability)
 */

interface DriverProfile {
  id: string;
  name: string;
  rating: number;
  trips: number;
  bio: string;
  specialties: string[];
  npsScore: number;
  kmDriven: number;
  experience: number;
  hourlyRate: number;
  status: 'available' | 'busy';
  badge: 'elite' | 'trusted' | 'standard';
}

const drivers: DriverProfile[] = [
  {
    id: '1',
    name: 'Samuel Tembo',
    rating: 4.99,
    trips: 2100,
    bio: 'Long-distance pro with 11 years of cross-country highway experience. Former police driving instructor — your safety is never a question with Samuel behind the wheel.',
    specialties: ['Long Distance Pro', 'Highway Specialist', 'Mountain Road Expert'],
    npsScore: 91,
    kmDriven: 148000,
    experience: 11,
    hourlyRate: 95,
    status: 'available',
    badge: 'elite',
  },
  {
    id: '2',
    name: 'Leila Nkosi',
    rating: 4.96,
    trips: 1560,
    bio: 'City navigation expert and former motorsport enthusiast turned professional chauffeur. Quietly confident, extraordinarily smooth, and deeply familiar with every major metro.',
    specialties: ['City Navigation Expert', 'Night Driver Expert', 'Luxury Car Handling'],
    npsScore: 85,
    kmDriven: 96000,
    experience: 8,
    hourlyRate: 90,
    status: 'available',
    badge: 'elite',
  },
  {
    id: '3',
    name: 'Marcus Osei',
    rating: 4.97,
    trips: 1240,
    bio: 'Calm and safety-focused with 9+ years of city and highway experience. Known for smooth handling, zero incidents, and treating every vehicle with absolute respect.',
    specialties: ['Highway Specialist', 'Luxury Car Handling', 'Executive Chauffeur'],
    npsScore: 82,
    kmDriven: 85000,
    experience: 9,
    hourlyRate: 85,
    status: 'available',
    badge: 'elite',
  },
  {
    id: '4',
    name: 'Fatima Diallo',
    rating: 4.94,
    trips: 890,
    bio: 'Night driving specialist with an exceptional sense of city navigation. Cool under pressure, certified in defensive driving, and deeply professional with corporate clients.',
    specialties: ['Night Driver Expert', 'City Navigation Expert', 'Corporate'],
    npsScore: 79,
    kmDriven: 62000,
    experience: 6,
    hourlyRate: 75,
    status: 'available',
    badge: 'trusted',
  },
];

export default function Home() {
  const [activeNav, setActiveNav] = useState('home');
  const [showMobileMenu, setShowMobileMenu] = useState(false);
  const [savedDrivers, setSavedDrivers] = useState<Set<string>>(new Set());
  const bookingFormRef = useRef<HTMLDivElement>(null);

  const toggleSaveDriver = (driverId: string) => {
    const newSaved = new Set(savedDrivers);
    if (newSaved.has(driverId)) {
      newSaved.delete(driverId);
    } else {
      newSaved.add(driverId);
    }
    setSavedDrivers(newSaved);
  };

  const scrollToBooking = () => {
    bookingFormRef.current?.scrollIntoView({ behavior: 'smooth' });
  };

  return (
    <div className="min-h-screen bg-background text-foreground overflow-x-hidden">
      {/* HEADER - Minimal Navigation */}
      <header className="fixed top-0 left-0 right-0 z-40 backdrop-blur-sm bg-background/80 border-b border-border/30">
        <div className="container flex items-center justify-between py-4">
          <div className="flex items-center gap-2">
            <div className="w-8 h-8 rounded-full bg-gradient-to-br from-accent to-accent/60 flex items-center justify-center">
              <span className="text-xs font-bold text-primary-foreground">D</span>
            </div>
            <span className="text-lg font-semibold tracking-wide" style={{ fontFamily: 'var(--font-display)' }}>DRIVORA</span>
          </div>
          
          <nav className="hidden md:flex items-center gap-8">
            {['How It Works', 'Drivers', 'Services', 'Safety'].map((item) => (
              <a
                key={item}
                href="#"
                className="text-sm font-medium text-foreground/70 hover:text-foreground transition-colors duration-300"
              >
                {item}
              </a>
            ))}
          </nav>

          <div className="hidden md:block">
            <Button
              onClick={scrollToBooking}
              className="bg-accent hover:bg-accent/90 text-primary-foreground rounded-full px-6 font-medium transition-all duration-300"
            >
              Book Now
            </Button>
          </div>

          {/* Mobile menu toggle */}
          <button
            onClick={() => setShowMobileMenu(!showMobileMenu)}
            className="md:hidden p-2 hover:bg-muted rounded-lg transition-colors"
          >
            {showMobileMenu ? <X size={20} /> : <Menu size={20} />}
          </button>
        </div>

        {/* Mobile menu */}
        {showMobileMenu && (
          <div className="md:hidden border-t border-border/30 bg-background/95 backdrop-blur-sm">
            <nav className="container py-4 flex flex-col gap-3">
              {['How It Works', 'Drivers', 'Services', 'Safety'].map((item) => (
                <a
                  key={item}
                  href="#"
                  className="text-sm font-medium text-foreground/70 hover:text-foreground py-2 transition-colors"
                >
                  {item}
                </a>
              ))}
              <Button
                onClick={() => {
                  scrollToBooking();
                  setShowMobileMenu(false);
                }}
                className="w-full bg-accent hover:bg-accent/90 text-primary-foreground rounded-full mt-2"
              >
                Book Now
              </Button>
            </nav>
          </div>
        )}
      </header>

      {/* HERO SECTION */}
      <section className="pt-32 pb-20 md:pt-40 md:pb-32 relative overflow-hidden">
        <div
          className="absolute inset-0 bg-cover bg-center opacity-40 -z-10"
          style={{
            backgroundImage: 'url(https://d2xsxph8kpxj0f.cloudfront.net/310519663565488987/59Ad69u5fBQpHJNjgwW3ws/drivora-hero-luxury-5i4AFDkyKuyrkBa4hbwdup.webp)',
          }}
        />
        <div className="absolute inset-0 bg-gradient-to-b from-background/60 via-background/40 to-background -z-10" />

        <div className="container max-w-4xl">
          <div className="mb-6 inline-block">
            <span className="text-xs font-medium text-accent tracking-widest uppercase px-3 py-1 rounded-full bg-accent/10 border border-accent/20">
              Personal Driver Marketplace
            </span>
          </div>

          <h1 className="text-5xl md:text-6xl lg:text-7xl font-bold leading-tight mb-6" style={{ fontFamily: 'var(--font-display)' }}>
            Your car.
            <br />
            <span className="text-accent">Our trusted</span>
            <br />
            drivers.
          </h1>

          <p className="text-lg md:text-xl text-foreground/70 max-w-2xl mb-8 leading-relaxed">
            Hire verified professional drivers to drive <em>your own vehicle</em>. Not a taxi. Not a ride-share. A personal driver experience — on your terms.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 mb-12">
            <Button
              onClick={scrollToBooking}
              className="bg-accent hover:bg-accent/90 text-primary-foreground rounded-full px-8 py-6 text-base font-medium transition-all duration-300 shadow-lg hover:shadow-xl"
            >
              Book a Driver <ChevronRight size={18} className="ml-2" />
            </Button>
            <Button
              variant="outline"
              className="border-border/50 hover:bg-muted rounded-full px-8 py-6 text-base font-medium transition-all duration-300"
            >
              Meet Our Drivers
            </Button>
          </div>

          {/* Trust Stats */}
          <div className="grid grid-cols-3 gap-6 md:gap-12">
            <div className="text-center md:text-left">
              <div className="text-3xl md:text-4xl font-bold text-accent mb-1" style={{ fontFamily: 'var(--font-display)' }}>1,200+</div>
              <p className="text-sm text-foreground/60 uppercase tracking-wide">Verified Drivers</p>
            </div>
            <div className="text-center md:text-left">
              <div className="text-3xl md:text-4xl font-bold text-accent mb-1" style={{ fontFamily: 'var(--font-display)' }}>98%</div>
              <p className="text-sm text-foreground/60 uppercase tracking-wide">Satisfaction Rate</p>
            </div>
            <div className="text-center md:text-left">
              <div className="text-3xl md:text-4xl font-bold text-accent mb-1" style={{ fontFamily: 'var(--font-display)' }}>+81</div>
              <p className="text-sm text-foreground/60 uppercase tracking-wide">NPS Score</p>
            </div>
          </div>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section className="py-20 md:py-32 bg-gradient-to-b from-background to-secondary/20">
        <div className="container max-w-5xl">
          <div className="mb-16">
            <h2 className="text-4xl md:text-5xl font-bold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
              How <span className="text-accent">Drivora</span> works
            </h2>
            <p className="text-lg text-foreground/60">Three simple steps to your perfect driver</p>
          </div>

          <div className="grid md:grid-cols-3 gap-8 md:gap-6">
            {[
              {
                number: '01',
                title: 'Request a Driver',
                description: 'Tell us your location, when you need the driver, your car type, and how long you need them. We match based on specialization and NPS score.',
                icon: MapPin,
              },
              {
                number: '02',
                title: 'Choose Your Driver',
                description: 'Browse verified profiles with full stats — NPS scores, total trips, kilometres driven, and honest bios. Choose who you trust most.',
                icon: Users,
              },
              {
                number: '03',
                title: 'Driver Arrives & Drives',
                description: 'Your selected driver arrives at your location, drives your car — professionally and safely. You stay in full control of your vehicle.',
                icon: Car,
              },
            ].map((step, idx) => {
              const Icon = step.icon;
              return (
                <div key={idx} className="group">
                  <div className="mb-6 relative">
                    <div className="w-16 h-16 rounded-full bg-accent/10 border border-accent/30 flex items-center justify-center group-hover:bg-accent/20 transition-colors duration-300">
                      <Icon size={28} className="text-accent" />
                    </div>
                    <span className="absolute -top-2 -right-2 text-2xl font-bold text-accent/40" style={{ fontFamily: 'var(--font-display)' }}>
                      {step.number}
                    </span>
                  </div>
                  <h3 className="text-xl font-semibold mb-3" style={{ fontFamily: 'var(--font-display)' }}>{step.title}</h3>
                  <p className="text-foreground/60 leading-relaxed">{step.description}</p>
                </div>
              );
            })}
          </div>
        </div>
      </section>

      {/* DRIVERS SECTION */}
      <section className="py-20 md:py-32">
        <div className="container max-w-6xl">
          <div className="mb-16">
            <h2 className="text-4xl md:text-5xl font-bold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
              Meet our <span className="text-accent">professionals</span>
            </h2>
            <p className="text-lg text-foreground/60">Verified, trusted, and ready to drive</p>
          </div>

          <div className="grid md:grid-cols-2 gap-6 lg:gap-8">
            {drivers.map((driver) => (
              <div
                key={driver.id}
                className="group relative rounded-2xl overflow-hidden border border-border/50 hover:border-accent/30 transition-all duration-300 hover:shadow-lg"
              >
                {/* Background gradient */}
                <div
                  className="absolute inset-0 bg-cover bg-center opacity-30 group-hover:opacity-40 transition-opacity duration-300"
                  style={{
                    backgroundImage: 'url(https://d2xsxph8kpxj0f.cloudfront.net/310519663565488987/59Ad69u5fBQpHJNjgwW3ws/drivora-driver-profile-bg-3ExveifwUnvEFunq3apXMP.webp)',
                  }}
                />
                <div className="absolute inset-0 bg-gradient-to-br from-background/80 via-background/60 to-background/40" />

                {/* Content */}
                <div className="relative p-6 md:p-8">
                  <div className="flex items-start justify-between mb-4">
                    <div className="flex items-center gap-3">
                      <div className="w-12 h-12 rounded-full bg-gradient-to-br from-accent to-accent/60 flex items-center justify-center">
                        <span className="text-sm font-bold text-primary-foreground">
                          {driver.name.charAt(0)}
                        </span>
                      </div>
                      <div>
                        <h3 className="font-semibold text-lg" style={{ fontFamily: 'var(--font-display)' }}>{driver.name}</h3>
                        <div className="flex items-center gap-2">
                          <Star size={14} className="text-accent fill-accent" />
                          <span className="text-sm font-medium">{driver.rating}</span>
                          <span className="text-xs text-foreground/50">· {driver.trips.toLocaleString()}+ trips</span>
                        </div>
                      </div>
                    </div>
                    <button
                      onClick={() => toggleSaveDriver(driver.id)}
                      className="p-2 hover:bg-accent/10 rounded-full transition-colors duration-300"
                    >
                      <Heart
                        size={20}
                        className={`transition-colors duration-300 ${
                          savedDrivers.has(driver.id)
                            ? 'fill-accent text-accent'
                            : 'text-foreground/40 hover:text-accent'
                        }`}
                      />
                    </button>
                  </div>

                  <p className="text-sm text-foreground/70 mb-4 leading-relaxed">{driver.bio}</p>

                  <div className="flex flex-wrap gap-2 mb-6">
                    {driver.specialties.map((specialty) => (
                      <span
                        key={specialty}
                        className="text-xs px-3 py-1 rounded-full bg-accent/10 text-accent border border-accent/20"
                      >
                        {specialty}
                      </span>
                    ))}
                  </div>

                  <div className="grid grid-cols-4 gap-3 mb-6 pb-6 border-b border-border/30">
                    <div>
                      <div className="text-sm font-bold text-accent" style={{ fontFamily: 'var(--font-display)' }}>{driver.trips.toLocaleString()}+</div>
                      <p className="text-xs text-foreground/50 uppercase tracking-wide">Trips</p>
                    </div>
                    <div>
                      <div className="text-sm font-bold text-accent" style={{ fontFamily: 'var(--font-display)' }}>{driver.kmDriven.toLocaleString()}+</div>
                      <p className="text-xs text-foreground/50 uppercase tracking-wide">Km</p>
                    </div>
                    <div>
                      <div className="text-sm font-bold text-accent" style={{ fontFamily: 'var(--font-display)' }}>{driver.experience}yr</div>
                      <p className="text-xs text-foreground/50 uppercase tracking-wide">Experience</p>
                    </div>
                    <div>
                      <div className="text-sm font-bold text-accent" style={{ fontFamily: 'var(--font-display)' }}>+{driver.npsScore}</div>
                      <p className="text-xs text-foreground/50 uppercase tracking-wide">NPS</p>
                    </div>
                  </div>

                  <div className="flex items-center justify-between">
                    <div>
                      <span className="text-xs text-accent/70 uppercase tracking-wide">From</span>
                      <div className="text-lg font-bold" style={{ fontFamily: 'var(--font-display)' }}>${driver.hourlyRate}/hr</div>
                    </div>
                    <Button className="bg-accent hover:bg-accent/90 text-primary-foreground rounded-full px-6 font-medium transition-all duration-300">
                      Book {driver.name.split(' ')[0]}
                    </Button>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* TRUST & SAFETY */}
      <section className="py-20 md:py-32 bg-gradient-to-b from-secondary/20 to-background">
        <div className="container max-w-5xl">
          <div className="mb-16">
            <h2 className="text-4xl md:text-5xl font-bold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
              Built on <span className="text-accent">trust.</span>
              <br />
              Backed by data.
            </h2>
          </div>

          <div className="grid md:grid-cols-2 gap-8 lg:gap-12 mb-16">
            {[
              {
                icon: Shield,
                title: 'ID Verified Drivers',
                description: 'Every driver on Drivora undergoes rigorous identity verification. Government-issued ID, background checks, and biometric confirmation.',
              },
              {
                icon: Car,
                title: 'License Verified',
                description: 'We verify class of driving licence, validity, endorsements, and driving record — ensuring each driver is legally qualified.',
              },
              {
                icon: Star,
                title: 'NPS Trust System',
                description: 'After every trip, passengers rate their experience. Net Promoter Scores are calculated and publicly displayed — no hiding.',
              },
              {
                icon: MapPin,
                title: 'Live Trip Tracking',
                description: 'Share your live trip location with trusted contacts. Our platform logs route data for every journey — your safety is auditable.',
              },
            ].map((item, idx) => {
              const Icon = item.icon;
              return (
                <div key={idx} className="group">
                  <div className="w-14 h-14 rounded-full bg-accent/10 border border-accent/30 flex items-center justify-center mb-4 group-hover:bg-accent/20 transition-colors duration-300">
                    <Icon size={24} className="text-accent" />
                  </div>
                  <h3 className="text-xl font-semibold mb-3" style={{ fontFamily: 'var(--font-display)' }}>{item.title}</h3>
                  <p className="text-foreground/60 leading-relaxed">{item.description}</p>
                </div>
              );
            })}
          </div>

          {/* NPS Explanation */}
          <div className="bg-gradient-to-br from-accent/5 to-accent/10 border border-accent/20 rounded-2xl p-8 md:p-12">
            <h3 className="text-2xl font-bold mb-6" style={{ fontFamily: 'var(--font-display)' }}>The only metric that matters: <span className="text-accent">trust</span></h3>
            <p className="text-foreground/70 mb-8 leading-relaxed">
              After each trip, passengers rate: "How likely are you to recommend this driver?" on a scale of 0–10. We calculate: <strong>NPS = % Promoters (9–10) minus % Detractors (0–6).</strong> Drivers with NPS above +70 earn our Highly Trusted badge. Drivers above +80 with 500+ trips earn Elite status.
            </p>
            <div className="grid grid-cols-3 gap-4">
              <div className="text-center">
                <div className="text-3xl font-bold text-accent mb-2" style={{ fontFamily: 'var(--font-display)' }}>9–10</div>
                <p className="text-sm text-foreground/60">Promoters</p>
                <p className="text-xs text-foreground/50 mt-1">Count positively</p>
              </div>
              <div className="text-center">
                <div className="text-3xl font-bold text-accent mb-2" style={{ fontFamily: 'var(--font-display)' }}>7–8</div>
                <p className="text-sm text-foreground/60">Passives</p>
                <p className="text-xs text-foreground/50 mt-1">Neutral impact</p>
              </div>
              <div className="text-center">
                <div className="text-3xl font-bold text-accent mb-2" style={{ fontFamily: 'var(--font-display)' }}>0–6</div>
                <p className="text-sm text-foreground/60">Detractors</p>
                <p className="text-xs text-foreground/50 mt-1">Count negatively</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* BOOKING FORM */}
      <section ref={bookingFormRef} className="py-20 md:py-32 bg-gradient-to-b from-background to-secondary/30">
        <div className="container max-w-2xl">
          <div className="mb-12">
            <h2 className="text-4xl md:text-5xl font-bold mb-4" style={{ fontFamily: 'var(--font-display)' }}>
              Reserve your
              <br />
              driver in <span className="text-accent">minutes</span>
            </h2>
            <p className="text-lg text-foreground/60">Tell us your details and we'll match you with the best available verified driver.</p>
          </div>

          <div className="bg-white rounded-2xl border border-border/50 p-8 md:p-10 shadow-lg">
            <form className="space-y-6">
              <div>
                <label className="block text-sm font-medium mb-2">Pickup Location</label>
                <input
                  type="text"
                  placeholder="Enter your address or area"
                  className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300"
                />
              </div>

              <div className="grid md:grid-cols-2 gap-6">
                <div>
                  <label className="block text-sm font-medium mb-2">Date</label>
                  <input
                    type="date"
                    className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300"
                  />
                </div>
                <div>
                  <label className="block text-sm font-medium mb-2">Time</label>
                  <input
                    type="time"
                    className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300"
                  />
                </div>
              </div>

              <div className="grid md:grid-cols-2 gap-6">
                <div>
                  <label className="block text-sm font-medium mb-2">Duration</label>
                  <select className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300">
                    <option>2 hours</option>
                    <option>4 hours</option>
                    <option>6 hours</option>
                    <option>8 hours (Full Day)</option>
                    <option>Custom</option>
                  </select>
                </div>
                <div>
                  <label className="block text-sm font-medium mb-2">Car Type</label>
                  <select className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300">
                    <option>Sedan</option>
                    <option>SUV</option>
                    <option>Luxury Sedan</option>
                    <option>Luxury SUV</option>
                    <option>Pickup Truck</option>
                  </select>
                </div>
              </div>

              <div>
                <label className="block text-sm font-medium mb-2">Service Type</label>
                <select className="w-full px-4 py-3 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-accent/50 transition-all duration-300">
                  <option>Personal Driver</option>
                  <option>Event Driver</option>
                  <option>Corporate Driver</option>
                  <option>Emergency Driver</option>
                </select>
              </div>

              <Button className="w-full bg-accent hover:bg-accent/90 text-primary-foreground rounded-full py-4 text-base font-medium transition-all duration-300 shadow-lg hover:shadow-xl mt-8">
                Find My Driver <ChevronRight size={18} className="ml-2" />
              </Button>
            </form>

            <div className="mt-8 pt-8 border-t border-border/30 grid grid-cols-3 gap-4 text-center text-sm">
              <div>
                <div className="font-semibold text-accent mb-1">✓</div>
                <p className="text-foreground/60">Instant Matching</p>
              </div>
              <div>
                <div className="font-semibold text-accent mb-1">✓</div>
                <p className="text-foreground/60">Fixed Transparent Pricing</p>
              </div>
              <div>
                <div className="font-semibold text-accent mb-1">✓</div>
                <p className="text-foreground/60">Cancel Anytime</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-foreground/5 border-t border-border/30 py-12 md:py-16">
        <div className="container">
          <div className="grid md:grid-cols-4 gap-8 mb-12">
            <div>
              <div className="flex items-center gap-2 mb-4">
                <div className="w-6 h-6 rounded-full bg-gradient-to-br from-accent to-accent/60 flex items-center justify-center">
                  <span className="text-xs font-bold text-primary-foreground">D</span>
                </div>
                <span className="font-semibold" style={{ fontFamily: 'var(--font-display)' }}>DRIVORA</span>
              </div>
              <p className="text-sm text-foreground/60">Your personal driver marketplace.</p>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Product</h4>
              <ul className="space-y-2 text-sm text-foreground/60">
                <li><a href="#" className="hover:text-foreground transition-colors">How It Works</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Browse Drivers</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Services</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Trust</h4>
              <ul className="space-y-2 text-sm text-foreground/60">
                <li><a href="#" className="hover:text-foreground transition-colors">Safety Standards</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Driver Verification</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Insurance Policy</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Company</h4>
              <ul className="space-y-2 text-sm text-foreground/60">
                <li><a href="#" className="hover:text-foreground transition-colors">About</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Contact</a></li>
                <li><a href="#" className="hover:text-foreground transition-colors">Press</a></li>
              </ul>
            </div>
          </div>

          <div className="border-t border-border/30 pt-8 flex flex-col md:flex-row items-center justify-between text-sm text-foreground/50">
            <p>&copy; 2026 Drivora. All rights reserved.</p>
            <div className="flex gap-6 mt-4 md:mt-0">
              <a href="#" className="hover:text-foreground transition-colors">Terms of Service</a>
              <a href="#" className="hover:text-foreground transition-colors">Privacy Policy</a>
            </div>
          </div>
        </div>
      </footer>

      {/* FLOATING BOTTOM NAV - Mobile Only */}
      <nav className="fixed bottom-0 left-0 right-0 md:hidden z-50">
        <div className="container px-4 pb-4">
          <div className="backdrop-blur-xl bg-background/80 border border-border/50 rounded-2xl shadow-2xl">
            <div className="flex items-center justify-around py-3">
              {[
                { id: 'home', label: 'Home', icon: '🏠' },
                { id: 'drivers', label: 'Drivers', icon: '👥' },
                { id: 'book', label: 'Book', icon: '📅' },
                { id: 'saved', label: 'Saved', icon: '❤️' },
                { id: 'profile', label: 'Profile', icon: '👤' },
              ].map((item) => (
                <button
                  key={item.id}
                  onClick={() => setActiveNav(item.id)}
                  className={`flex flex-col items-center gap-1 px-3 py-2 rounded-lg transition-all duration-300 ${
                    activeNav === item.id
                      ? 'bg-accent/20 text-accent'
                      : 'text-foreground/50 hover:text-foreground'
                  }`}
                >
                  <span className="text-xl">{item.icon}</span>
                  <span className="text-xs font-medium">{item.label}</span>
                </button>
              ))}
            </div>
          </div>
        </div>
      </nav>

      {/* Add padding to prevent content overlap with floating nav */}
      <div className="h-24 md:h-0" />
    </div>
  );
}
